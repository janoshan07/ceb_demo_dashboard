---
kind: external_dependency
name: React 19 SPA frontend
slug: react
category: external_dependency
category_hints:
    - vendor_identity
scope:
    - '**'
---

The UI is a React 19 single-page application using Vite 8 as the build tool and react-router-dom 7 for client-side routing. It communicates with the Spring Boot backend over HTTP.