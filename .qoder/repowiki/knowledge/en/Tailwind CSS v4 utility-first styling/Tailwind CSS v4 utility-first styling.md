---
kind: external_dependency
name: Tailwind CSS v4 utility-first styling
slug: tailwind-css-v4
category: external_dependency
category_hints:
    - framework_behavior
scope:
    - '**'
---

Styling uses Tailwind CSS v4 with the new Vite integration package (`@tailwindcss/vite`). This is the v4 engine (not the legacy v3 postcss pipeline), which changes how configuration and purge settings work compared to older Tailwind setups.